#include <iostream>
using namespace std;
template <class T>
void swap(T a, T b)
{
    T temp;
    temp=a;
    a=b;
    b=temp;
    cout<<"\nAfter the swap:first no. is :"<<a<<" and second is: "<<b;
}
int main()
{
    swap(2,3);
    swap(3.3,2.8);

    return 0;
}
